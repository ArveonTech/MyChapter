const HeaderComponent = () => {
  return (
    <div className="bg-secondary pb-10 text-center px-10">
      <h1 className="pt-10">The archive page is a special space that holds items that have been moved by users so that they do not appear in the main list, but can still be accessed again whenever needed.</h1>
    </div>
  );
};

export default HeaderComponent;
